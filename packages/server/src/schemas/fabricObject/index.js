export default {
    id: { type: String, required: true },
    title: { type: String, required: true },
    description: String,
    img: String,
    props: Object,
    timeSpend: Number,
    cost: Number,
}